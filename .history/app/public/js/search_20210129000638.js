const serachModule = (() => {
    const BASE_URL = "http://localhost:3000/api/v1/serach"
    return {
        serachUsers: async () = {
            // 検索窓への入力値を取得
            const query = document.getElementById("search").value
        }
    }
})()